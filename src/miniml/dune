
(ocamllex (modules lexer))
(menhir (modules parser))

(executable
 (name miniml)
 (modules miniml)
 (libraries miniml)
 (modes native))

 (library
  (name miniml)
  (wrapped false)
  (flags -warn-error -a)
  (modules (:standard \ miniml))
  (modes native))
 